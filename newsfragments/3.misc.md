Added transport timeout and disconnect behavior tests for TCP and serial transports, including close failures, connection failures, write failures, partial writes, short reads, and timeout error mapping.
